#!/usr/bin/bash

OPTION=$(echo -e "Lock\nSleep\nReboot\nShutdown" | wofi --dmenu)

case "$OPTION" in
    "Lock") hyprlock >/dev/null ;;
    "Sleep") systemctl suspend ;;
    "Reboot") systemctl reboot ;;
    "Shutdown") systemctl poweroff ;;
    *) ;;
esac
